import React from "react";

/*
Required functionalities:
- Send and receive messages
- Clear sender vs receiver message bubbles
- Message timestamps
- Multiline input (Enter to send, Shift+Enter for new line)
- Auto-scroll to latest message
- Scroll-to-bottom when user is not at bottom
- Empty, loading, and error states
- Typing / sending indicator
- Retry failed messages
- Support links

*/
const ChatWindow = () => {
  return (
    <div>
      <h1>this is chat window !</h1>
    </div>
  );
};

export default ChatWindow;
